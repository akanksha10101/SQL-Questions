SELECT u.user_id
FROM rc_users u
LEFT JOIN rc_calls c
  ON u.user_id = c.user_id 
     AND c.call_date >= '2020-04-01' AND c.call_date < '2020-05-01'
WHERE u.status = 'free' 
  AND c.call_id IS NULL;
